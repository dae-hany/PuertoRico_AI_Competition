# Core (engine, environment, tournament) — required
numpy>=1.21
gymnasium>=0.28
pettingzoo>=1.24,<1.26      # AgentSelector API; suite verified on 1.24.x and 1.25.0
trueskill>=0.4.5            # TrueSkill leaderboard metric

# Reinforcement-learning baseline + training — optional
# (only needed for agents/ppo_agent.py and training/train_ppo.py;
#  with pyproject: pip install -e ".[rl]")
torch>=1.13

# Web debug UI — optional (webui/server.py)
flask>=2.0
